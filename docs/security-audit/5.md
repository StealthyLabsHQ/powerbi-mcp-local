New visual writers bypass readonly policy
Link: <https://chatgpt.com/codex/cloud/security/findings/8b3be641e148819197fb70e424c423dc?sev=critical%2Chigh&repo=https%3A%2F%2Fgithub.com%2FStealthyLabsHQ%2Fpowerbi-mcp-local>
Criticality: high (attack path: high)
Status: new

# Metadata

Repo: StealthyLabsHQ/powerbi-mcp-local
Commit: d3b23bc
Author: <125747450+stealthsrc@users.noreply.github.com>
Created: 5/8/2026, 9:47:18 PM
Assignee: Unassigned
Signals: Security, Validated, Patch generated, Attack-path

# Summary

Introduced: two newly registered tools perform write operations but are omitted from WRITE_TOOLS, causing the category enforcement to misclassify them as read tools.
The new pbi_set_visual_format_property and pbi_disable_card_autoscale tools both modify the extracted report Layout and call _save_layout(), but they were not added to WRITE_TOOLS or DESTRUCTIVE_TOOLS in src/security.py. SECURITY.validate_tool_call relies on tool_category(), which defaults unknown tools to "read". As a result, when the server is started with --readonly while keeping the default --profile all, or when a security policy denies write/destructive categories, these new write operations are still permitted because they are misclassified as reads. This bypasses the intended readonly/write-deny control and allows an MCP caller to alter report visuals in a supposedly read-only deployment.

# Validation

## Rubric

- [x] Confirm the two suspected tools are actual registered MCP wrappers and pass the exact names `pbi_set_visual_format_property` and `pbi_disable_card_autoscale` to security validation.
- [x] Confirm both tool implementations perform write operations by mutating visual configuration and calling `_save_layout()`.
- [x] Confirm both tool names are omitted from `WRITE_TOOLS` and `DESTRUCTIVE_TOOLS`, causing `tool_category()` to fall through to `read`.
- [x] Confirm readonly/write-deny enforcement depends on `tool_category()` and blocks a known write control while allowing the suspect tools.
- [x] Confirm live exploitation changes a report `Report/Layout` in readonly mode through the real server wrapper path.

## Report

Validated the finding at commit d3b23bcaba8a162d16ea91fa1a4af2d958e15b24. This is a Python authorization/category-enforcement bug, so crash/ASan/Valgrind are not applicable; `valgrind` was unavailable and there is no native memory-corruption target to instrument. Code review plus a live PoC and pdb trace confirmed the issue.

Key code evidence: `src/server.py:149-160` routes MCP wrapper calls through `SECURITY.validate_tool_call(tool_name, kwargs)`. The new wrappers are registered at `src/server.py:1841-1894` using names `pbi_set_visual_format_property` and `pbi_disable_card_autoscale`. Their implementations mutate and save report Layout data: `src/tools/visuals.py:1729-1752` updates `singleVisual.objects[...]`, rewrites `container['config']`, and calls `_save_layout`; `src/tools/visuals.py:1787-1822` updates card label properties and calls `_save_layout`. Security classification is list-based: `src/security.py:71-128` contains `WRITE_TOOLS` but omits both new tool names; `src/security.py:363-383` blocks readonly/write-deny based on `category`; `src/security.py:401-410` falls through to `return 'read'` for unknown tools.

Dynamic reproduction: `/workspace/validation_work/readonly_bypass_poc.py` stubs only the missing FastMCP decorator package, imports the real server/security/tool code, enables `SECURITY` readonly mode, creates a minimal extracted `Report/Layout`, and calls both wrappers. Output showed both wrappers registered and both categorized as read while a control write tool was blocked: `pbi_add_card_control: write`, `pbi_set_visual_format_property: read`, `pbi_disable_card_autoscale: read`, `known_write_blocked_by_readonly: true`, `known_write_error: Readonly mode blocks tool 'pbi_add_card'.` Despite `readonly_policy: true`, both suspect calls returned ok and changed the Layout hash. The resulting Layout contained the injected title object with literal `'changed despite readonly'` and labels object with `labelDisplayUnits: 1.0D` and `labelPrecision: 2.0D`.

Additional deny-write check: `/workspace/validation_work/deny_write_output.txt` shows a policy with `deny_categories: ['write']` has `policy_allow_write: false`; it blocks `pbi_add_card` with `Write tools are disabled by the active security policy` but allows `pbi_set_visual_format_property` because its category is `read`.

Debugger evidence: non-interactive pdb break at `src/security.py:365` during `validate_tool_call('pbi_set_visual_format_property', {})` showed `tool_name == 'pbi_set_visual_format_property'`, then after stepping category computation, `category == 'read'` and `policy.readonly == True`; execution continued and returned without `SecurityPolicyError`.

# Evidence

src/security.py (L363 to 410)
  Note: Readonly and allow/deny enforcement depends on tool_category(); tools absent from WRITE_TOOLS and DESTRUCTIVE_TOOLS fall through to return "read", bypassing write-category blocking.

```
    def validate_tool_call(self, tool_name: str, params: dict[str, Any]) -> SecurityPolicy:
        policy = self.policy()
        category = tool_category(tool_name, params)
        if policy.enabled_tools is not None and tool_name not in policy.enabled_tools:
            raise SecurityPolicyError(f"Tool '{tool_name}' is not enabled by the active security policy.")
        if tool_name in policy.disabled_tools:
            raise SecurityPolicyError(f"Tool '{tool_name}' is disabled by the active security policy.")
        if policy.readonly and category in {"write", "destructive"}:
            raise SecurityPolicyError(
                f"Readonly mode blocks tool '{tool_name}'.",
                details={"tool": tool_name, "category": category},
            )
        if category == "read" and not policy.allow_read:
            raise SecurityPolicyError(f"Read tools are disabled by the active security policy.")
        if category == "write" and not policy.allow_write:
            raise SecurityPolicyError(f"Write tools are disabled by the active security policy.")
        if category == "destructive" and not policy.allow_destructive:
            raise SecurityPolicyError(f"Destructive tools are disabled by the active security policy.")
        self._record_call(tool_name, policy)
        _validate_params(tool_name, params, policy)
        return policy

    def _record_call(self, tool_name: str, policy: SecurityPolicy) -> None:
        now = time.time()
        with self._lock:
            while self._calls and now - self._calls[0] > 60:
                self._calls.popleft()
            self._calls.append(now)
            count = len(self._calls)
        if policy.warn_after_calls_per_minute and count > policy.warn_after_calls_per_minute:
            self._logger.warning("SECURITY: high MCP tool call volume detected (%d calls/minute, latest=%s)", count, tool_name)
        if policy.rate_limit_calls_per_minute is not None and count > policy.rate_limit_calls_per_minute:
            raise SecurityPolicyError(
                "MCP tool call rate limit exceeded.",
                details={"count_last_minute": count, "limit": policy.rate_limit_calls_per_minute},
            )


def tool_category(tool_name: str, params: dict[str, Any] | None = None) -> str:
    if tool_name == "pbi_export_model" and params and not params.get("path"):
        return "read"
    if tool_name in {"pbi_excel_import_workflow", "pbi_measure_workflow", "pbi_repair_report_fields"}:
        return "write" if params and params.get("apply") else "read"
    if tool_name in DESTRUCTIVE_TOOLS:
        return "destructive"
    if tool_name in WRITE_TOOLS:
        return "write"
    return "read"
```

src/security.py (L71 to 128)
  Note: WRITE_TOOLS contains existing report-layout writers, but the newly added pbi_set_visual_format_property and pbi_disable_card_autoscale entries are missing.

```
WRITE_TOOLS = {
    "pbi_create_measure",
    "pbi_create_measures",
    "pbi_create_relationship",
    "pbi_update_relationship",
    "pbi_rename_table",
    "pbi_rename_column",
    "pbi_rename_measure",
    "pbi_set_format",
    "pbi_refresh",
    "pbi_import_dax_file",
    "pbi_create_table",
    "pbi_create_column",
    "pbi_export_model",
    "pbi_create_role",
    "pbi_set_role_filter",
    "pbi_add_role_member",
    "pbi_remove_role_member",
    "pbi_create_calc_group",
    "pbi_add_visual",
    "pbi_set_power_query",
    "pbi_create_import_query",
    "pbi_create_csv_import_query",
    "pbi_create_folder_import_query",
    "pbi_bulk_import_excel",
    "pbi_import_excel_workbook",
    "pbi_write_tmdl_file",
    "pbi_patch_tmdl_measure",
    "pbi_create_persistent_report",
    "pbi_export_validation_report",
    "excel_write_cell",
    "excel_write_range",
    "excel_create_sheet",
    "excel_format_range",
    "excel_auto_width",
    "excel_create_workbook",
    "pbi_extract_report",
    "pbi_compile_report",
    "pbi_patch_layout",
    "pbi_create_page",
    "pbi_set_page_size",
    "pbi_add_card",
    "pbi_add_bar_chart",
    "pbi_add_line_chart",
    "pbi_add_donut_chart",
    "pbi_add_table_visual",
    "pbi_add_waterfall",
    "pbi_add_slicer",
    "pbi_add_gauge",
    "pbi_add_text_box",
    "pbi_move_visual",
    "pbi_apply_design",
    "pbi_apply_theme",
    "pbi_build_dashboard",
    "pbi_excel_import_workflow",
    "pbi_measure_workflow",
    "pbi_repair_report_fields",
}
```

src/server.py (L1841 to 1894)
  Note: The commit registers the two new MCP tools and routes them through SECURITY.validate_tool_call using the names pbi_set_visual_format_property and pbi_disable_card_autoscale.

```
@mcp.tool()
def pbi_set_visual_format_property(
    extract_folder: str,
    page: str,
    visual_id: str,
    object_name: str,
    properties: dict[str, Any],
    property_types: dict[str, str] | None = None,
) -> dict[str, Any]:
    """Override format properties on an existing visual.

    object_name examples: ``title``, ``categoryAxis``, ``valueAxis``,
    ``labels``, ``dataPoint``, ``general``. Properties are merged into
    ``singleVisual.objects[object_name][0].properties`` and encoded to PBI's
    canonical literal forms (single-quoted text, ``L`` int suffix, ``D``
    decimal suffix, ``#RRGGBB`` solid color, ``true``/``false`` bool).

    Pass property_types to force an encoding ("text", "bool", "int",
    "decimal", "color", or "raw" for pre-shaped expr dicts).
    """
    return _run(
        "pbi_set_visual_format_property",
        pbi_set_visual_format_property_tool,
        extract_folder=extract_folder,
        page=page,
        visual_id=visual_id,
        object_name=object_name,
        properties=properties,
        property_types=property_types,
    )


@mcp.tool()
def pbi_disable_card_autoscale(
    extract_folder: str,
    page: str | None = None,
    visual_ids: list[str] | None = None,
    label_precision: int = 0,
) -> dict[str, Any]:
    """Disable Power BI's auto K/M unit-scaling on card visuals.

    Sets ``labelDisplayUnits=1`` (None) and an explicit ``labelPrecision``
    on every card. Use to fix the "119K K €" double-suffix bug where a
    measure already pre-divides by 1000 with a ``K €`` format string and
    PBI auto-scales on top.
    """
    return _run(
        "pbi_disable_card_autoscale",
        pbi_disable_card_autoscale_tool,
        extract_folder=extract_folder,
        page=page,
        visual_ids=visual_ids,
        label_precision=label_precision,
    )
```

src/server.py (L2350 to 2365)
  Note: --readonly sets runtime readonly policy, while the default profile remains all; therefore category misclassification is enough for these tools to remain callable in readonly mode.

```
    parser.add_argument(
        "--readonly",
        action="store_true",
        help="Disable write and destructive tools for this server process.",
    )
    parser.add_argument(
        "--profile",
        choices=["readonly", "write", "all"],
        default="all",
        help="Filter exposed tool surface: readonly, write (read+write), or all (default).",
    )
    args = parser.parse_args()
    SECURITY.policy(reload=True, cwd=Path(__file__).parent)
    if args.readonly:
        SECURITY.set_runtime_readonly(True)
        logger.info("SECURITY: readonly mode enabled via --readonly")
```

src/tools/visuals.py (L1696 to 1762)
  Note: pbi_set_visual_format_property mutates the visual config and writes the modified Layout via _save_layout().

```
def pbi_set_visual_format_property_tool(
    extract_folder: str,
    page: str,
    visual_id: str,
    object_name: str,
    properties: dict[str, Any],
    property_types: dict[str, str] | None = None,
) -> dict[str, Any]:
    """Set formatting properties on an existing visual's ``singleVisual.objects[<object_name>][0].properties``.

    Use to override titles, axis labels, data labels, etc. on a visual that's
    already on the report — without rebuilding it. Encodes Python values as
    proper Power BI literals (text in single quotes, ints with ``L`` suffix,
    decimals with ``D`` suffix, ``#RRGGBB`` as solid color, bool as
    ``true``/``false``).

    ``property_types`` lets you force the encoding of a specific property:
    ``"text"``, ``"bool"``, ``"int"``, ``"decimal"``, ``"color"``, or ``"raw"``
    (pass-through).

    Existing properties under the same object are preserved and merged with
    the new values (write-through semantics).
    """
    def _impl() -> dict[str, Any]:
        if not object_name or not str(object_name).strip():
            raise PowerBIValidationError("object_name must be non-empty.")
        if not isinstance(properties, dict) or not properties:
            raise PowerBIValidationError(
                "properties must be a non-empty dict of {property_name: value}.",
                details={"properties": repr(properties)},
            )
        types_map = {k: str(v) for k, v in (property_types or {}).items()}

        folder, layout = _load_layout(extract_folder)
        section = _find_page(layout, page)
        _, container, config = _find_visual(section, visual_id)
        single_visual = config.setdefault("singleVisual", {})
        objects = single_visual.setdefault("objects", {})
        existing = objects.get(object_name) or [{"properties": {}}]
        if not isinstance(existing, list) or not existing:
            existing = [{"properties": {}}]
        merged_props = dict(existing[0].get("properties", {}))

        encoded: dict[str, Any] = {}
        for prop_name, raw_value in properties.items():
            if not prop_name or not str(prop_name).strip():
                raise PowerBIValidationError(
                    "property names must be non-empty strings.",
                    details={"name": repr(prop_name)},
                )
            hint = types_map.get(prop_name)
            encoded[prop_name] = _encode_visual_format_value(raw_value, hint=hint)
        merged_props.update(encoded)
        existing[0]["properties"] = merged_props
        objects[object_name] = existing
        container["config"] = _dump_embedded_json(config)
        _save_layout(folder, layout)
        return ok(
            f"Format properties applied to visual '{visual_id}' (object '{object_name}').",
            extract_folder=str(folder),
            page=str(section.get("displayName") or section.get("name")),
            visual_id=visual_id,
            object=object_name,
            applied=sorted(encoded.keys()),
        )

    return _run(_impl)
```

src/tools/visuals.py (L1765 to 1831)
  Note: pbi_disable_card_autoscale mutates card visual properties and writes the modified Layout via_save_layout().

```
def pbi_disable_card_autoscale_tool(
    extract_folder: str,
    page: str | None = None,
    visual_ids: list[str] | None = None,
    label_precision: int = 0,
) -> dict[str, Any]:
    """Disable the auto K/M/B unit-scaling on card visuals.

    By default Power BI cards display large numeric values rescaled (e.g.
    ``119,229`` becomes ``119K``). When the underlying measure already uses a
    custom format string with ``K`` / ``€`` suffixes the result is the
    classic "119K K €" double-suffix bug. This tool sets ``labelDisplayUnits=1``
    (None) plus an explicit ``labelPrecision`` on every card on the report
    (or restricted to ``page`` / ``visual_ids``).

    ``label_precision``: 0 keeps integer display, raise to 2 if you need the
    decimal portion of the underlying measure to render.
    """
    def _impl() -> dict[str, Any]:
        if visual_ids is not None and not isinstance(visual_ids, list):
            raise PowerBIValidationError("visual_ids must be a list of visual ids or None.")
        ids_filter = {str(v).strip() for v in (visual_ids or []) if str(v).strip()}
        folder, layout = _load_layout(extract_folder)

        target_sections: list[dict[str, Any]]
        if page:
            target_sections = [_find_page(layout, page)]
        else:
            target_sections = list(layout.get("sections", []) or [])

        patched: list[dict[str, Any]] = []
        for section in target_sections:
            for container in section.get("visualContainers", []) or []:
                cfg_raw = container.get("config")
                if not isinstance(cfg_raw, str):
                    continue
                cfg = json.loads(cfg_raw)
                sv = cfg.get("singleVisual", {})
                if sv.get("visualType") != "card":
                    continue
                visual_id = str(cfg.get("name", ""))
                if ids_filter and visual_id not in ids_filter:
                    continue
                objects = sv.setdefault("objects", {})
                labels = objects.get("labels")
                if not isinstance(labels, list) or not labels:
                    labels = [{"properties": {}}]
                props = dict(labels[0].get("properties", {}))
                props["labelDisplayUnits"] = _decimal_literal(1)  # 1 = None
                props["labelPrecision"] = _decimal_literal(int(label_precision))
                labels[0]["properties"] = props
                objects["labels"] = labels
                container["config"] = _dump_embedded_json(cfg)
                patched.append({
                    "visual_id": visual_id,
                    "page": str(section.get("displayName") or section.get("name", "")),
                })
        _save_layout(folder, layout)
        return ok(
            f"Disabled autoscale on {len(patched)} card visual(s).",
            extract_folder=str(folder),
            patched=patched,
            patched_count=len(patched),
            label_precision=int(label_precision),
        )

    return _run(_impl)
```

Proposed patch:
diff --git a/src/security.py b/src/security.py
index 5ddaba5baaa454c8fb98b5c4963ddedf0f13aae2..dd316085a8cd76d255aa870c4ac37d12475e5da6 100644
--- a/src/security.py
+++ b/src/security.py
@@ -139,50 +139,52 @@ WRITE_TOOLS = {
     "pbi_write_tmdl_file",
     "pbi_patch_tmdl_measure",
     "pbi_create_persistent_report",
     "pbi_export_validation_report",
     "excel_write_cell",
     "excel_write_range",
     "excel_create_sheet",
     "excel_format_range",
     "excel_auto_width",
     "excel_create_workbook",
     "pbi_extract_report",
     "pbi_compile_report",
     "pbi_patch_layout",
     "pbi_create_page",
     "pbi_set_page_size",
     "pbi_add_card",
     "pbi_add_bar_chart",
     "pbi_add_line_chart",
     "pbi_add_donut_chart",
     "pbi_add_table_visual",
     "pbi_add_waterfall",
     "pbi_add_slicer",
     "pbi_add_gauge",
     "pbi_add_text_box",
     "pbi_move_visual",
- "pbi_set_visual_format_property",
- "pbi_disable_card_autoscale",
     "pbi_update_visual_bindings",
     "pbi_apply_design",
     "pbi_apply_theme",
     "pbi_build_dashboard",
     "pbi_excel_import_workflow",
     "pbi_measure_workflow",
     "pbi_repair_report_fields",
     "pbi_persist_now",
 }

 DESTRUCTIVE_TOOLS = {
     "pbi_delete_measure",
     "pbi_delete_relationship",
     "pbi_delete_table",
     "pbi_delete_column",
     "pbi_delete_role",
     "pbi_delete_calc_group",
     "excel_delete_sheet",
     "pbi_delete_page",
     "pbi_remove_visual",
 }

 EXCEL_TOOLS = {
     "excel_list_sheets",
     "excel_read_sheet",

# Attack-path analysis

Final: high | Decider: model_decided | Matrix severity: medium | Policy adjusted: medium

## Rationale

Kept as high. Static evidence and validation PoC evidence show a real authorization/category bypass in the main runtime: registered write-capable visual tools are misclassified as read and can save Layout changes under --readonly or deny-write policy. This is important in this project's threat model because readonly/profile controls are the intended mitigation against prompt-injected or untrusted MCP calls. It is not raised to critical because default exposure is local/localhost, exploitation requires MCP-call reachability and accessible report files, and the impact is report-layout integrity rather than code execution, credential compromise, or destructive cross-boundary control.

## Likelihood

medium - The affected wrappers are registered in the normal tool surface and --profile all is the default, so the bug is straightforward for any MCP caller that can trigger tool calls. Likelihood is moderated by the local-first deployment model, default stdio/localhost exposure, and the need for access to an extracted report path.

## Impact

high - A caller who should be limited to read-only behavior can write report Layout visual properties and card label settings. This compromises report integrity and can misrepresent BI outputs, but the demonstrated effect is limited to local report-layout modification and does not prove RCE, credential theft, or broad destructive model control.

## Assumptions

- The MCP server is run with the default --profile all while enabling --readonly, or with a security_policy.json/PBI_MCP_SECURITY_POLICY that denies the write category but does not explicitly disable these two tool names.
- An attacker can influence MCP tool calls through an MCP client, prompt-injection path, local client integration, or optional SSE transport.
- The target extracted report folder is reachable under the server's allowed local path policy.
- Registered MCP tool surface includes pbi_set_visual_format_property or pbi_disable_card_autoscale.
- Readonly or deny-write policy is relied on for authorization.
- Default --profile all is used, or profile pruning does not remove the affected tools.
- Attacker can cause a tool call with parameters pointing at an accessible extracted Power BI report layout.

## Path

MCP caller -> registered wrapper -> SECURITY.validate_tool_call -> tool_category() returns read -> visual tool writes Layout -> readonly/write-deny bypass

## Path evidence

- `src/server.py:149-160` - _run() is the central wrapper path for MCP tool calls and invokes SECURITY.validate_tool_call() before executing the callback.
- `src/server.py:1841-1894` - The affected tools are registered as MCP tools and are passed to _run() under the names pbi_set_visual_format_property and pbi_disable_card_autoscale.
- `src/tools/visuals.py:1696-1752` - pbi_set_visual_format_property_tool mutates visual objects/properties, rewrites container['config'], and calls_save_layout().
- `src/tools/visuals.py:1765-1822` - pbi_disable_card_autoscale_tool mutates card label properties, rewrites container['config'], and calls _save_layout().
- `src/security.py:71-128` - WRITE_TOOLS lists many report-layout writers but omits pbi_set_visual_format_property and pbi_disable_card_autoscale.
- `src/security.py:363-383` - Readonly and category-deny enforcement depends on the category returned by tool_category().
- `src/security.py:401-410` - tool_category() only returns write/destructive for listed names and otherwise falls through to read.
- `src/server.py:2328-2387` - The default transport is stdio, optional SSE binds to 127.0.0.1:8765 by default, --readonly sets runtime readonly, and --profile defaults to all.
- `src/server.py:2449-2464` - --profile readonly/write can prune tools based on category sets, but --profile all does not prune and is the default.

## Narrative

The finding is real and reachable. The new MCP wrappers are registered and routed through _run(), which calls SECURITY.validate_tool_call(). Their implementations edit the embedded visual configuration and call_save_layout(), so they are write operations. However, both tool names are missing from WRITE_TOOLS and DESTRUCTIVE_TOOLS. Because tool_category() returns read for any name not found in those sets, readonly and deny-write enforcement do not trigger. This is in the main product runtime and matches the threat model's concern about MCP callers or prompt-injected workflows bypassing policy controls. The impact is an integrity compromise of Power BI report visual layouts in supposedly read-only deployments; it is not critical because the default exposure is local/localhost, exploitation requires MCP tool-call reachability and an accessible report extract, and the demonstrated sinks are report-layout writes rather than RCE, credential access, or broad destructive model changes.

## Controls

- SECURITY.validate_tool_call authorization hook
- --readonly runtime flag
- security_policy allow_categories/deny_categories
- --profile readonly/write tool pruning
- default stdio transport
- default SSE host 127.0.0.1 and optional PBI_MCP_AUTH_TOKEN Bearer auth
- allowed_base_dirs/local path validation
- audit logging and optional rate limit

## Blindspots

- No deployment manifests were present to prove whether any real installation exposes SSE beyond localhost.
- Static review did not enumerate every possible policy configuration; explicit enabled_tools/disabled_tools or --profile readonly can mitigate this specific path.
- Business impact depends on how the modified reports are consumed or trusted downstream.
- Other uncategorized mutating tools may exist and would require a broader registry/category audit.
